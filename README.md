# Secure Vault

> [!NOTE]
> The `gate` crate acts as a facade and re-exports the dependencies.

> [!CAUTION]
> This system is in the earliest of stages. Things are going to change without bumping
> serialization versions until the first release.
